// This file is intentionally left minimal.
// The app uses SafepathApp defined in main.dart.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Safepath smoke test', (WidgetTester tester) async {
    // Basic smoke test placeholder – full tests require Firebase mock setup
    expect(true, isTrue);
  });
}
